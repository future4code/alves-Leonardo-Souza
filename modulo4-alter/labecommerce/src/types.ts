type users = {
    id: string,
    name: string,
    email: string,
    password: string
}

export default users

export type products = {
    id: string,
    name: string,
    price: number,
    img_url: string
}